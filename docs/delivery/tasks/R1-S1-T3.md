# R1-S1-T3 - Prevent writes after candidate erasure

Status: in progress (split 2026-09-10). PI/sprint: PI-R1 / R1-S1. Finding: F05.
Prerequisites: T1 and T2 complete. See ../WORKFLOW.md.

The scope spans independent report, interview and derived-store transaction
boundaries. Complete one bounded child per chat; do not close this parent until
all children and required database evidence pass.

1. **R1-S1-T3a**: report/outcome persistence and evaluation cancellation.
   Controlled competing connections, safe refusal logging, HTTP/restart journey.
   Implemented; awaiting PostgreSQL validation (retry 2026-09-11).
   Validation retry: `.env` test connection fails password authentication;
   correct the disposable connection before running database tests.
   Evidence: [R1-S1-T3a.md](R1-S1-T3a.md).
2. **R1-S1-T3b**: interview and derived-store stale writes. Trace fingerprints,
   feature vectors, matching/screening retained data and interview start/turn/
   completion; add controlled transaction regressions and defined API failures.
   Include cross-store ordering and any remaining candidate-ingest race.
   Entry points: interview/store.py, candidates/store.py, features/store.py,
   matching/store.py, screening/store.py, portal/service.py and their tests.
   Create a child evidence record before implementation; split further if needed.

Both children require focused/full pytest, self-review, relevant HTTP/restart
journeys and isolated SQLite/PostgreSQL concurrency evidence. No schema change
is planned; migration up/down/up applies if implementation introduces one.
Parent acceptance: erasure cannot be undone by stale candidate-linked work;
other candidates remain intact and clients receive defined refusals/cancellation.
T3b remains pending; F05 and R1-S1-Q remain open.
