"""S7.3 AI interview delivery.

Pure logic (questions / scoring / proxy / session) with persistence and
orchestration layered on top -- the app/verification/ shape. The interview asks
the depth report's OWN probes, so this package reads reports and profiles; it
never writes to them.
"""
